# Copy this file to .env and fill in your real values.
# .env should NOT be committed — it's already covered in .gitignore.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=bank_management
